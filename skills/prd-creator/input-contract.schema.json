{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "prd-creator-input-contract",
  "title": "PRD-Creator Input Contract",
  "description": "Defines the input parameters for the PRD-Creator skill, supporting both direct and orchestrated modes.",
  "type": "object",
  "required": ["mode", "initialRequest"],
  "properties": {
    "mode": {
      "type": "string",
      "enum": ["direct", "orchestrated"],
      "description": "Operational mode. Direct: interacts with the user. Orchestrated: invoked by another agent with structured context."
    },
    "initialRequest": {
      "type": "string",
      "minLength": 10,
      "description": "The raw user request or initial problem statement."
    },
    "upstream_context": {
      "type": ["string", "null"],
      "description": "Upstream artifact content for orchestrated mode. Priority order: brd.md → brainstorm.md → raw-requirements.md → null."
    },
    "contextSchema": {
      "type": "object",
      "description": "Contextual information for the PRD (primarily used in Orchestrated mode).",
      "properties": {
        "product": {
          "type": "string",
          "description": "The product or application name."
        },
        "module": {
          "type": "string",
          "description": "The specific module or feature area."
        },
        "techStack": {
          "type": "array",
          "items": { "type": "string" },
          "description": "List of technologies to be used."
        },
        "projectRules": {
          "type": "array",
          "items": { "type": "string" },
          "description": "List of project-specific rules or guidelines."
        }
      }
    },
    "constraints": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Explicit constraints (architectural, technical, design) for the feature."
    },
    "metadata": {
      "type": "object",
      "description": "Additional metadata about the request.",
      "properties": {
        "output_path": {
          "type": "string",
          "description": "Absolute path where prd.md should be written (orchestrated mode). The orchestrator reads from this path after each invocation."
        },
        "priority": {
          "type": "string",
          "enum": ["low", "medium", "high"]
        },
        "riskLevel": {
          "type": "string",
          "enum": ["low", "medium", "high"]
        }
      }
    },
    "current_draft": {
      "type": "string",
      "description": "Current prd.md content read from output_path. Present only in refinement mode."
    },
    "reviewer_findings": {
      "type": "array",
      "description": "Problems returned by the reviewer. Present only in refinement mode. When present, prd-creator must refine current_draft rather than regenerating.",
      "items": {
        "type": "object",
        "required": ["id", "location", "severity", "description"],
        "properties": {
          "id":          { "type": "string" },
          "location":    { "type": "string" },
          "severity":    { "type": "string", "enum": ["blocker", "major", "minor"] },
          "description": { "type": "string" }
        }
      }
    },
    "upstream_warnings": {
      "type": "array",
      "description": "DONE_WITH_CONCERNS notes propagated from prior pipeline stages.",
      "items": { "type": "string" }
    }
  }
}
