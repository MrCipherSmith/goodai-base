# Optional: Grok paths / sync target

## Problem
Decide whether extra Grok integration is needed after Day 1 strategy A.

## Expected Outcome
Gate decision recorded: Option 7 (local paths) / Option 8 (sync target) / neither.
No unnecessary code or PR if Claude-compat smoke already works.

## Out of Scope
Reopening strategy A validator work; machine-absolute paths committed to repo.
